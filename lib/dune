(library
  (name Lib)
  (libraries
    tyre lambdasoup
    ssl lwt_ssl cohttp-lwt-unix
  )
)

;(rule
;   (target version.ml)
;   (action
;    (with-stdout-to %{target}
;    (echo "let git_sha = \"foo")
;     (run git rev-parse --short HEAD)
;    (echo "bar\"")
;   ))
;  )
